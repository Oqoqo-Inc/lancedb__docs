{/* Auto-generated by scripts/mdx_snippets_gen.py. Do not edit manually. */}

export const PyAuthOauthConnect = "from typing import Dict\n\nimport lancedb\nfrom lancedb.remote.header import OAuthProvider\n\n\ndef fetch_token() -> Dict[str, object]:\n    return {\n        'access_token': 'example-oauth-access-token',\n        'expires_in': 3600,\n    }\n\n\nprovider = OAuthProvider(fetch_token, refresh_buffer_seconds=300)\n\ndb = lancedb.connect(\n    uri='db://your-database-uri',\n    api_key='your-api-key',\n    region='us-east-1',\n    header_provider=provider,\n)\n";

export const PyAuthOauthNoExpiry = "from typing import Dict\n\nimport lancedb\nfrom lancedb.remote.header import OAuthProvider\n\n\ndef fetch_token() -> Dict[str, object]:\n    return {\n        'access_token': 'example-non-expiring-token',\n    }\n\n\nprovider = OAuthProvider(fetch_token)\n\ndb = lancedb.connect(\n    uri='db://your-database-uri',\n    api_key='your-api-key',\n    region='us-east-1',\n    header_provider=provider,\n)\n";

export const TsAuthOauthProvider = "import { OAuthHeaderProvider, type TokenResponse } from '@lancedb/lancedb';\n\nasync function fetchToken(): Promise<TokenResponse> {\n  const response = await fetch('https://oauth.example.com/token', {\n    method: 'POST',\n    headers: { 'Content-Type': 'application/json' },\n    body: JSON.stringify({\n      grant_type: 'client_credentials',\n      client_id: process.env.CLIENT_ID,\n      client_secret: process.env.CLIENT_SECRET,\n    }),\n  });\n\n  const data = await response.json();\n  return {\n    accessToken: data.access_token,\n    expiresIn: data.expires_in,\n  };\n}\n\nexport const oauthProvider = new OAuthHeaderProvider(fetchToken, 5 * 60);\n";

export const TsAuthOauthConnect = "import { connect } from '@lancedb/lancedb';\nimport { oauthProvider } from './oauthProvider';\n\nconst db = await connect(\n  'db://your-database-uri',\n  {\n    apiKey: process.env.LANCEDB_API_KEY!,\n    region: process.env.LANCEDB_REGION ?? 'us-east-1',\n  },\n  undefined,\n  oauthProvider,\n);\n";

export const TsAuthOauthFunctionProvider = "import { connect, type TokenResponse } from '@lancedb/lancedb';\n\nasync function fetchToken(): Promise<TokenResponse> {\n  const response = await fetch('https://oauth.example.com/token', {\n    method: 'POST',\n    headers: { 'Content-Type': 'application/json' },\n    body: JSON.stringify({\n      grant_type: 'client_credentials',\n      client_id: process.env.CLIENT_ID,\n      client_secret: process.env.CLIENT_SECRET,\n    }),\n  });\n\n  const data = await response.json();\n  return {\n    accessToken: data.access_token,\n    expiresIn: data.expires_in,\n  };\n}\n\nconst db = await connect(\n  'db://your-database-uri',\n  {\n    apiKey: process.env.LANCEDB_API_KEY!,\n    region: process.env.LANCEDB_REGION ?? 'us-east-1',\n  },\n  undefined,\n  async () => {\n    const token = await fetchToken();\n    return {\n      Authorization: `Bearer ${token.accessToken}`,\n    };\n  },\n);\n";

export const TsAuthOauthStaticHeader = "import { StaticHeaderProvider, connect } from '@lancedb/lancedb';\n\nconst staticProvider = new StaticHeaderProvider({\n  Authorization: 'Bearer example-static-token',\n  'X-Request-Source': 'docs-example',\n});\n\nconst db = await connect(\n  'db://your-database-uri',\n  {\n    apiKey: process.env.LANCEDB_API_KEY!,\n    region: process.env.LANCEDB_REGION ?? 'us-east-1',\n  },\n  undefined,\n  staticProvider,\n);\n";
